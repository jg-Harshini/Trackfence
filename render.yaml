services:
  # Backend Service (Spring Boot via Docker)
  - type: web
    name: dementia-tracker-backend
    env: docker
    region: singapore
    plan: free
    rootDirectory: backend
    dockerFilePath: Dockerfile
    envVars:
      - key: SPRING_DATA_MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: SHIPDAY_API_KEY
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        fromService:
          name: dementia-tracker-frontend
          type: web # Changed to web for compatibility
          property: url

  # Frontend Service (React Vite)
  - type: web # Using web + env: static for broader compatibility
    name: dementia-tracker-frontend
    env: static
    rootDirectory: frontend
    buildCommand: npm run build
    staticPublishPath: dist # Using the correct key for static sites
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          name: dementia-tracker-backend
          type: web
          property: url
